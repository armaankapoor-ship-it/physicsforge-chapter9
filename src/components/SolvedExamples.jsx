import { Calculator, CheckCircle2, ClipboardList } from 'lucide-react'
import { solvedExamples } from '../data/solvedExamples'
import { AlertBox, ConceptCard, FormulaBox, SectionShell, Tag } from './ui'
export default function SolvedExamples() {
  return <SectionShell id="solved-examples" eyebrow="Solved examples" title="Exam-style solved examples with traps called out." description="These examples cover the fastest scoring numerical and conceptual patterns before the full practice bank."><div className="grid gap-5 md:grid-cols-2">{solvedExamples.map((example,index) => <article key={example.title} className="glass-card p-5 md:p-6"><div className="flex flex-wrap items-center gap-2"><Tag>{String(index+1).padStart(2,'0')}</Tag><Tag>{example.tag}</Tag></div><h3 className="mt-4 text-xl font-black tracking-tight text-ink dark:text-white">{example.title}</h3><p className="mt-3 text-sm font-semibold leading-7 text-slate-600 dark:text-slate-300">{example.problem}</p><div className="mt-4 grid gap-2">{example.steps.map((step, stepIndex) => <div key={step} className="flex gap-3 rounded-2xl bg-slate-50 p-3 text-sm font-semibold leading-6 text-slate-700 dark:bg-white/[.045] dark:text-slate-200"><CheckCircle2 className="mt-1 shrink-0 text-teal-600 dark:text-teal-300" size={16} /><span><strong>Step {stepIndex+1}:</strong> {step}</span></div>)}</div><div className="mt-4 grid gap-3"><FormulaBox title="Final answer">{example.answer}</FormulaBox><AlertBox title="Common trap" tone="trap">{example.trap}</AlertBox></div></article>)}</div><div className="mt-8 grid gap-4 md:grid-cols-3"><ConceptCard icon={Calculator} kicker="Numericals" title="Formula chain" color="blue">Each solved example shows model choice, unit conversion and formula use.</ConceptCard><ConceptCard icon={ClipboardList} kicker="Boards" title="Writing style" color="amber">The steps are short enough to reproduce cleanly in board answers.</ConceptCard><ConceptCard icon={CheckCircle2} kicker="Traps" title="Mistake-aware" color="teal">Every solution ends with the most likely exam mistake.</ConceptCard></div></SectionShell>
}
